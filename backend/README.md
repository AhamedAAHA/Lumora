# Lumora Backend

Express REST API with MongoDB, JWT auth, OpenAI, and ElevenLabs.

## Commands

```bash
npm install
npm run dev      # http://localhost:5000
npm start
```

## Environment

Copy `.env.example` to `.env` and configure `MONGODB_URI`, `JWT_SECRET`, and API keys.
